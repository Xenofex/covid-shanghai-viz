{
  "name": "shanghai_viz",
  "version": "1.0.0",
  "description": "",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs && node geocode.mjs",
    "server": "((sleep 0.5; open 'http://localhost:8000/visualize.html') &) && cd www/ && python3 -m http.server",
    "deploy": "rsync -C -r www/ fs:/var/www/covid-shanghai/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jsdom": "^19.0.0",
    "node-fetch": "^3.2.3"
  },
  "devDependencies": {
    "esbuild": "^0.14.29"
  }
}
